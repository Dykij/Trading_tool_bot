"""
Модуль-обертка для конфигурации.

Этот модуль импортирует настройки из src/config/config.py для обеспечения
совместимости с различными частями проекта, которые импортируют config напрямую.
"""

# Импортируем все из модуля src.config.config
from src.config.config import *

# Создаем экземпляр конфигурации в глобальной области видимости
# для импорта в виде "from config import config"
config = Config() 

def load_config():
    """
    Возвращает экземпляр конфигурации.
    
    Returns:
        Config: Экземпляр класса конфигурации
    """
    return config 